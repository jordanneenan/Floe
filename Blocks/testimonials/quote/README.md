# Quote card (`floe/quote`)

One short quote in **Testimonials**: quote (Body L; type the curly quotes), name, role and an optional round portrait (in the card or the sidebar). Nothing renders if the quote is empty.
